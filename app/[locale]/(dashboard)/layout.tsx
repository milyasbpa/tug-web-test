// TODO: Add sidebar, auth guard, and dashboard layout (Step 7 - Authentication Flow)
export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
