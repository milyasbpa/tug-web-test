// TODO: Step 3 — Implement use-login wrapper
// Wraps useAuthControllerLogin from @/core/api/generated/auth/auth
// Adds: onSuccess (setAuth + session cookie + router.push) + onError (toast)

export {};
