'use client';

// TODO: Step 7 — Implement delete confirmation dialog
// Reads selectedPackage + isDeleteOpen from usePackagesStore
// On confirm → useDeletePackage
export function DeleteDialogPackages() {
  return null;
}
