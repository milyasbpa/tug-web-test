'use client';

// TODO: Step 4 — Implement packages table
// Fetches data via useAdminPackages (react-query/)
// On Edit click → usePackagesStore.openEditModal(pkg)
// On Delete click → usePackagesStore.openDeleteDialog(pkg)
// On "Add Package" click → usePackagesStore.openCreateModal()
export function TablePackages() {
  return null;
}
