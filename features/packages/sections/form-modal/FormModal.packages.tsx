'use client';

// TODO: Step 5/6 — Implement package form modal (create + edit)
// Reads modalMode + selectedPackage from usePackagesStore
// create → useCreatePackage, edit → useUpdatePackage
// Schema: form-modal.packages.schema.ts (colocated)
export function FormModalPackages() {
  return null;
}
